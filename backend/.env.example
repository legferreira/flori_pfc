# Copie para .env e preencha. O .env nunca vai para o git.

# Deixe em branco para usar MySQL (padrão do projeto).
# Use DB_ENGINE=sqlite para rodar local sem instalar MySQL.
DB_ENGINE=

DB_NAME=flori_db
DB_USER=
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306
